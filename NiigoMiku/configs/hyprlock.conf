# sample hyprlock.conf
# for more configuration options, refer https://wiki.hyprland.org/Hypr-Ecosystem/hyprlock
#
# rendered text in all widgets supports pango markup (e.g. <b> or <i> tags)
# ref. https://wiki.hyprland.org/Hypr-Ecosystem/hyprlock/#general-remarks
#
# shortcuts to clear password buffer: ESC, Ctrl+U, Ctrl+Backspace
#
# you can get started by copying this config to ~/.config/hypr/hyprlock.conf
#

$font = HeavyData NerdFont

general {
    hide_cursor = true
}

# uncomment to enable fingerprint authentication
# auth {
#     fingerprint {
#         enabled = true
#         ready_message = Scan fingerprint to unlock
#         present_message = Scanning...
#         retry_delay = 250 # in milliseconds
#     }
# }

animations {
    enabled = true
    bezier = linear, 1, 1, 0, 0
    animation = fadeIn, 1, 5, linear
    animation = fadeOut, 1, 5, linear
    animation = inputFieldDots, 1, 2, linear
}

background {
    monitor =
    path = /home/kuki/Documents/Themes/NiigoMiku/images/mikupastel.jpeg
    blur_passes = 0
}

input-field {
    monitor =
    size = 3.5%, 6%
    outline_thickness = 3
    inner_color = rgba(255, 255, 255, 0.45) # no fill

    outer_color = rgba(235, 232, 153, 1.0) rgba(235, 232, 206, 1.0) 195deg
    check_color = rgba(152, 218, 240, 1.0) rgba(235, 232, 153, 1.0) 330deg
    fail_color = rgba(ff6633ee) rgba(ff0066ee) 40deg

    font_color = rgb(0, 0, 0)
    fade_on_empty = true
    rounding = 100

    font_family = $font
    placeholder_text = 
    fail_text = $PAMFAIL

    # uncomment to use a letter instead of a dot to indicate the typed password
    dots_text_format = 󰣇
    dots_size = 0.6
    dots_spacing = 0.3
    font_color = rgba(152, 218, 240, 1.0)

    # uncomment to use an input indicator that does not show the password length (similar to swaylock's input indicator)
    #hide_input = true

    position = -50, -380
    halign = center
    valign = center
}

# TIME
label {
    monitor =
    text = $TIME # ref. https://wiki.hyprland.org/Hypr-Ecosystem/hyprlock/#variable-substitution
    font_size = 300
    font_family = $font

    position = 0, -20
    halign = center
    valign = top
}

# DATE
label {
    monitor =
    text = cmd[update:60000] date +"%A, %d %B %Y" # update every 60 seconds
    font_size = 55
    font_family = $font

    position = 0, -20
    halign = center
    valign = top
}

#label {
#    monitor =
#    font_family = $font
#    text = cmd[update:1000] playerctl metadata title
#    font_size = 30
    #onclick = 

#    position = 0, 0
#    halign = right
#    valign = center
}

#label {
#	monitor=
	#text = cmd[update:1] ~/.config/hypr/scripts/eq.sh
#	position = 0, 0
#	halign = right
#	valign = center
#}
